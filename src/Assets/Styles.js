import {StyleSheet} from 'react-native';
export const CustomButtonStyling = StyleSheet.create({
  buttonCreate: {
    borderWidth: 1,
    borderRadius: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
  iconContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
});
